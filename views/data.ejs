<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>Here the data is located</h1>

    <h2>
      <%= users.map((user) => { return (`${user.name} ${user.email}`) }) %>
    </h2>
  </body>
</html>
